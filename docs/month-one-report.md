# Dani Bot — 30-day report

Period:
Version tagged:

## What we shipped
Bullet facts only (desktop installers / `pnpm dev:desktop`, This computer, pairing scheme). Not a live hosted demo.

## Interviews
Count. OS mix. Install success rate. Top 3 blockers.

## Computer-use
Pane = watch-only. Agent vision = CUA screenshots. Allow = chat broker.
What worked / what was architecture (no MCP ⇒ no Allow).

## Browser (Webcmd)
Commands reused vs live `browser run`. Token/turn cost if measured; otherwise qualitative.
Do not recommend wrapping every Playwright API as MCP tools.

## Mobile
`danibot://pair`. Rebuild required. HTTP `/pair` as fallback.

## Next
Ship this tag. Keep Cloud Box / Local VM **out** of the Computer panel.
